"""Give a single command that computes the sum from Exercise R-1.4, rely-
ing on Python’s comprehension syntax and the built-in sum function."""


print(sum([num*num for num in range(1, 5)]))    #30